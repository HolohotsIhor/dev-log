# LLM provider: "openai" | "anthropic"
LLM_PROVIDER=openai

# Your API key (leave empty to use mock mode)
LLM_API_KEY=

# Set to "true" to use deterministic mock responses (no real API calls)
LLM_MOCK=true

# Model to use (e.g. gpt-4o, claude-3-5-sonnet-20241022)
LLM_MODEL=gpt-4o
